# Showing the mock component values

This is just a simple temporary code run exercise that will extract and print a few representative values from the component trait implementation we just constructed.

